# ⚠️ Rename this file to `.env` and update the values before running the project.

# 🌍 Application Configuration
PORT=":3000"  # API server port

# 🛢️ Database Configuration
DB_HOST="127.0.0.1"  # Change to `mariadb` if using Docker
DB_PORT=3306         # Default MySQL/MariaDB port
DB_NAME="NS_db"      # Database name
DB_USER="user"       # Database username
DB_PASSWORD="0000"   # Database password

# 📦 Goose Migration Config
GOOSE_DRIVER="mysql"
GOOSE_DBSTRING="user:0000@tcp(127.0.0.1:3306)/NS_db"  # Connection string for Goose
GOOSE_MIGRATION_DIR="./migration"
